const saleState={cart:[],orderId:null,paymentMethod:'',idempotencyKey:null,sellerId:null,sellerName:null};const $=selector=>document.querySelector(selector);function saleMoney(value){return `Bs ${Number(value||0).toFixed(2)}`}function currentLayout(){return document.querySelector('.sale-layout')}function initSeller(){const layout=currentLayout();saleState.sellerId=layout?.dataset.currentSellerId||'';saleState.sellerName=layout?.dataset.currentSellerName||'-';updateSellerLabel()}function updateSellerLabel(){const label=$('#cartSellerLabel');if(label)label.textContent=`Vendedor: ${saleState.sellerName||'-'}${saleState.sellerId?` · ID ${saleState.sellerId}`:''}`}function newIdempotencyKey(){if(window.crypto&&crypto.randomUUID)return crypto.randomUUID();return `${Date.now()}-${Math.random().toString(16).slice(2)}`}function isVendible(product){return Number(product.vendible||0)===1&&product.presentacion_id&&product.precio_venta_estandar!==null&&product.precio_minimo_venta!==null}function cashIsOpen(){return currentLayout()?.dataset.cashOpen==='1'}function normalizeItem(product){return{producto_id:product.producto_id,presentacion_id:product.presentacion_id,nombre:product.nombre,codigo_producto:product.codigo_producto,presentacion:product.presentacion||'Unidad',cantidad:1,precio_estandar:Number(product.precio_venta_estandar),precio_minimo:Number(product.precio_minimo_venta),precio_unitario:Number(product.precio_venta_estandar),stock_total:parseInt(product.stock_total||0,10),stock_local:parseInt(product.stock_local||0,10)}}function addToCart(product){if(!isVendible(product)){alert('Este producto todavía no tiene precio o presentación configurada.');return}const existing=saleState.cart.find(item=>item.presentacion_id===product.presentacion_id);if(existing){existing.cantidad+=1}else{saleState.cart.push(normalizeItem(product))}saleState.orderId=null;renderCart()}function totals(){return saleState.cart.reduce((acc,item)=>{acc.subtotal+=item.precio_estandar*item.cantidad;acc.total+=item.precio_unitario*item.cantidad;return acc},{subtotal:0,total:0,discount:0})}function renderCart(){const container=$('#cartItems');if(!container)return;updateSellerLabel();if(!saleState.cart.length){container.innerHTML='<p class="empty">Añade productos al carrito.</p>'}else{container.innerHTML=saleState.cart.map((item,index)=>`<article class="cart-item" style="padding:8px 0"><div class="cart-item__title" style="gap:6px;align-items:flex-start"><div style="min-width:0;flex:1"><div style="display:flex;gap:6px;align-items:center"><button class="remove-item" type="button" data-remove-item="${index}" aria-label="Eliminar producto" title="Eliminar" style="width:26px;height:26px;line-height:1;padding:0;border-radius:8px">🗑</button><strong style="font-size:13px;font-weight:500">${item.nombre}</strong></div><small>${item.presentacion} · ${item.codigo_producto||''}</small></div><span style="font-size:12px">${saleMoney(item.precio_unitario*item.cantidad)}</span></div><div class="cart-item__controls" style="gap:4px;align-items:center"><button class="btn btn--ghost" type="button" data-qty-minus="${index}" aria-label="Restar cantidad" style="width:28px;height:28px;padding:0">−</button><input type="number" min="1" step="1" value="${item.cantidad}" data-cart-qty="${index}" aria-label="Cantidad" style="max-width:52px;height:28px;text-align:center;padding:2px 4px"><button class="btn btn--ghost" type="button" data-qty-plus="${index}" aria-label="Sumar cantidad" style="width:28px;height:28px;padding:0">+</button><input type="number" min="${item.precio_minimo}" max="${item.precio_estandar}" step="0.01" value="${item.precio_unitario}" data-cart-price="${index}" aria-label="Precio unitario" style="max-width:88px;height:28px;padding:2px 4px"></div><small>Estándar ${saleMoney(item.precio_estandar)} · mínimo ${saleMoney(item.precio_minimo)} · stock sucursal ${item.stock_local}</small></article>`).join('')}const t=totals();t.discount=t.subtotal-t.total;$('#cartSubtotal').textContent=saleMoney(t.subtotal);$('#cartDiscount').textContent=saleMoney(t.discount);$('#cartTotal').textContent=saleMoney(t.total)}async function searchProducts(){const q=$('#saleSearch').value.trim();const response=await fetch(`/ventas/api/productos?q=${encodeURIComponent(q)}`);const products=await response.json();const results=$('#saleResults');results.innerHTML=products.map(product=>{const vendible=isVendible(product);const stockLocal=parseInt(product.stock_local||0,10);const otras=product.stock_otras_ubicaciones||'Sin disponibilidad en otras ubicaciones';const stockClass=stockLocal>0?'stock':'stock danger-text';return `<article class="product-card"><div class="product-card__top"><div><h3>${product.nombre}</h3><span class="code">${product.codigo_producto||''} · ${product.presentacion||'Unidad'}</span></div><span class="price">${vendible?saleMoney(product.precio_venta_estandar):'Sin precio'}</span></div><small>${product.descripcion||''}</small><span class="${stockClass}" title="Otras sucursales/almacenes: ${otras}">Stock en esta sucursal: ${stockLocal}</span><small title="${product.stock_ubicaciones||''}">Pasa el mouse para ver otras disponibilidades.</small>${vendible?`<button class="btn btn--ghost" type="button" data-add-product='${JSON.stringify(product).replaceAll("'","&apos;")}'>Añadir</button>`:'<button class="btn btn--ghost" type="button" disabled>Falta precio</button>'}</article>`}).join('')||'<p class="empty">No se encontraron productos.</p>'}function openCheckout(){if(!cashIsOpen()){alert('Debes abrir caja antes de confirmar ventas.');document.querySelector('#cashOpenModal')?.classList.add('is-open');return}if(!saleState.cart.length){alert('Añade productos antes de confirmar.');return}saleState.idempotencyKey=newIdempotencyKey();const preview=$('#checkoutPreview');preview.innerHTML=`<div class="checkout-row"><span>Vendedor</span><span>${saleState.sellerName||'-'}${saleState.sellerId?` · ID ${saleState.sellerId}`:''}</span></div>`+saleState.cart.map(item=>`<div class="checkout-row"><span>${item.cantidad} × ${item.nombre} (${item.presentacion})</span><span>${saleMoney(item.precio_unitario*item.cantidad)}</span></div>`).join('')+`<div class="checkout-row"><span>Total</span><span>${saleMoney(totals().total)}</span></div>`;$('#saleResult').hidden=true;$('#confirmSaleBtn').disabled=false;$('#confirmSaleBtn').textContent='Confirmar pago';$('#checkoutModal').classList.add('is-open')}function payloadItems(){return saleState.cart.map(item=>({producto_id:item.producto_id,presentacion_id:item.presentacion_id,cantidad:item.cantidad,precio_unitario:item.precio_unitario}))}async function sendToCashier(){if(!saleState.cart.length){alert('Añade productos antes de enviar a caja.');return}const button=$('#sendCashierBtn');button.disabled=true;try{const response=await fetch('/ventas/ordenes/enviar-caja',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({items:payloadItems()})});const data=await response.json();if(!response.ok)throw new Error(data.error||'No se pudo enviar a caja.');saleState.cart=[];renderCart();alert(`Pedido enviado a caja. Orden #${data.order_id}`)}catch(error){alert(error.message)}finally{button.disabled=false}}async function confirmSale(){if(!cashIsOpen()){alert('Debes abrir caja antes de confirmar ventas.');return}if(!saleState.paymentMethod){alert('Selecciona un método de pago.');return}const button=$('#confirmSaleBtn');button.disabled=true;button.textContent='Procesando...';try{const response=await fetch('/ventas/confirmar',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({items:saleState.orderId?[]:payloadItems(),orden_id:saleState.orderId,metodo_pago:saleState.paymentMethod,idempotency_key:saleState.idempotencyKey})});const data=await response.json();if(!response.ok)throw new Error(data.error||'No se pudo confirmar la venta.');const comprobante=data.numero_comprobante?`Comprobante ${String(data.numero_comprobante).padStart(6,'0')}`:data.numero_venta;$('#saleResult').hidden=false;$('#saleResult').innerHTML=`Venta confirmada: <span>${comprobante}</span> por ${saleMoney(data.total)}.<br><a href="/ventas/${data.venta_id}/comprobante" target="_blank">Imprimir comprobante</a> · <a href="/ventas/${data.venta_id}/comprobante-pdf" target="_blank">Abrir PDF</a>`;button.textContent='Venta confirmada';button.disabled=true;saleState.cart=[];saleState.orderId=null;initSeller();renderCart();searchProducts()}catch(error){alert(error.message);button.disabled=false;button.textContent='Confirmar pago'}}async function loadOrder(orderId){const response=await fetch(`/ventas/api/ordenes/${orderId}`);const order=await response.json();if(!response.ok){alert(order.error||'No se pudo cargar la orden.');return}saleState.orderId=order.id;saleState.sellerId=order.vendedor_id||saleState.sellerId;saleState.sellerName=order.vendedor_nombre||order.vendedor_username||saleState.sellerName;saleState.cart=order.detalles.map(item=>({producto_id:item.producto_id,presentacion_id:item.producto_presentacion_id,nombre:item.nombre,codigo_producto:item.codigo_producto,presentacion:item.presentacion,cantidad:parseInt(item.cantidad||1,10),precio_estandar:Number(item.precio_estandar),precio_minimo:Number(item.precio_minimo_venta),precio_unitario:Number(item.precio_unitario),stock_total:0,stock_local:0}));renderCart();window.scrollTo({top:0,behavior:'smooth'})}document.addEventListener('DOMContentLoaded',()=>{initSeller();renderCart();searchProducts();$('#saleSearch')?.addEventListener('input',debounce(searchProducts,220));$('#saleResults')?.addEventListener('click',event=>{const button=event.target.closest('[data-add-product]');if(!button)return;addToCart(JSON.parse(button.dataset.addProduct))});$('#cartItems')?.addEventListener('input',event=>{const qtyIndex=event.target.dataset.cartQty;const priceIndex=event.target.dataset.cartPrice;if(qtyIndex!==undefined){saleState.cart[qtyIndex].cantidad=Math.max(parseInt(event.target.value||1,10),1);event.target.value=saleState.cart[qtyIndex].cantidad}if(priceIndex!==undefined){const item=saleState.cart[priceIndex];const price=Number(event.target.value||item.precio_estandar);item.precio_unitario=Math.min(Math.max(price,item.precio_minimo),item.precio_estandar);event.target.value=item.precio_unitario}saleState.orderId=null;renderCart()});$('#cartItems')?.addEventListener('click',event=>{const minus=event.target.closest('[data-qty-minus]');const plus=event.target.closest('[data-qty-plus]');const remove=event.target.closest('[data-remove-item]');if(minus){const index=Number(minus.dataset.qtyMinus);saleState.cart[index].cantidad=Math.max(1,(saleState.cart[index].cantidad||1)-1);saleState.orderId=null;renderCart();return}if(plus){const index=Number(plus.dataset.qtyPlus);saleState.cart[index].cantidad=(saleState.cart[index].cantidad||1)+1;saleState.orderId=null;renderCart();return}if(remove){saleState.cart.splice(Number(remove.dataset.removeItem),1);saleState.orderId=null;renderCart()}});$('#clearCartBtn')?.addEventListener('click',()=>{saleState.cart=[];saleState.orderId=null;initSeller();renderCart()});$('#checkoutBtn')?.addEventListener('click',openCheckout);$('#sendCashierBtn')?.addEventListener('click',sendToCashier);$('#confirmSaleBtn')?.addEventListener('click',confirmSale);$('#openCashModalBtn')?.addEventListener('click',()=>document.querySelector('#cashOpenModal')?.classList.add('is-open'));$('#openCloseCashModal')?.addEventListener('click',()=>document.querySelector('#cashCloseModal')?.classList.add('is-open'));document.querySelector('[data-choice-group="payment"]')?.addEventListener('choice:change',event=>{saleState.paymentMethod=event.detail.value});document.querySelectorAll('[data-load-order]').forEach(button=>{button.addEventListener('click',()=>loadOrder(button.dataset.loadOrder))})});